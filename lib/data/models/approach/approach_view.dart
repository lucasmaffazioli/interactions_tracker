// import 'package:floor/floor.dart';

// @DatabaseView('''
// SELECT approach.id, approach.dateTime, approach.name, approach.description
// FROM approach
// LEFT JOIN approach_points ON approach.id = approach_points.approachId
// LEFT JOIN point ON point.id = approach_points.pointId
// ORDER BY approach.dateTime
// ''', viewName: 'approachList')
// //
// class ApproachView {
//   // final String name;

//   // Name(this.name);
// }
